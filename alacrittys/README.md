# alacritty

### USAGE
- By default you can into `vi` mode using `ctrl` `shift` `space`.You can start a selection using `v` and copy it using `y`.
- Search,`Ctrl Shift f`,backward using `Ctrl shift b` or `Ctrl c`
